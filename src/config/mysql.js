// Konfigurasi database
let dbConfig = {
    multipleStatements: true,
    host: "localhost",
    user: "root",
    password: "",
    database: "db_spkm",
};

// Eksport Konfigurasi Koneksi MySQL
module.exports = dbConfig;